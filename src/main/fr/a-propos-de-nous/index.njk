---
title: À propos de nous
description: Rencontrez l'équipe de spécialistes de l'accessibilité à l'origine de la boîte à outils sur l'accessibilité numérique.
layout: layouts/base.njk
toggle: about-us
tags: main
---

{% if collections.aboutUs === undefined %}

	{% include "partials/alert-landingpages.njk" %}

{% else %}

<div class="row wb-eqht mrgn-tp-lg">
	{% for item in collections.aboutUs | localeMatch(locale) %}
	<div class="col-md-6">
		<h3><a href="{{ item.url }}">{{ item.data.title }}</a></h3>
		<p>{{ item.data.description }}</p>
	</div>
	{% endfor %}
</div>

{% endif %}
