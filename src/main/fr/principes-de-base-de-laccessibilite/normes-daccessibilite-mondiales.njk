---
title: Normes d’accessibilité mondiales
description: Les lignes directrices visant à garantir que le contenu numérique est accessible dans le monde entier aux personnes vivant avec un handicap et des cadres pour créer des produits et services numériques accessibles, etc.
layout: layouts/base.njk
toggle: global-accessibility-standards
tags: accessibilityFundamentals
---

{% if collections.globalAccessibilityStandards === undefined %}

	{% include "partials/alert-landingpages.njk" %}

{% else %}

<div class="row wb-eqht mrgn-tp-lg gc-drmt">
	{% for item in collections.globalAccessibilityStandards | sort(false, false, 'data.title') | localeMatch(locale) %}
	<div class="col-md-6">
		<h2><a href="{% if item.data.redirect %}{{ item.data.redirect }}{% else %}{{ item.url }}{% endif %}" {% if item.data.oneLanguage %} hreflang="{{ landingPage[locale].otherLang }}" {% endif %}>{{ item.data.title | safe }}{% if item.data.oneLanguage %} {{ landingPage[locale].otherLangOnly }}{% endif %}</a></h2>
		<p>
			{% if item.data.description %}
			{{ item.data.description | safe }}
			{% else %}
			{{ landingPage[locale].descriptionNoneText }}
			{% endif %}
		</p>
	</div>
	{% endfor %}
</div>

{% endif %}
