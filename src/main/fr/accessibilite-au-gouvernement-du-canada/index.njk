---
title: Accessibilité au gouvernement du Canada
description: En savoir plus sur les normes d'accessibilité, les rôles et les équipes liées au gouvernement du Canada.
layout: layouts/base.njk
toggle: accessibility-in-the-government-of-canada
tags: main
---

{% if collections.accessibilityInTheGovernmentOfCanada === undefined %}

	{% include "partials/alert-landingpages.njk" %}

{% else %}

<div class="row wb-eqht mrgn-tp-lg">
	{% for item in collections.accessibilityInTheGovernmentOfCanada | localeMatch(locale) %}
	<div class="col-md-6">
		<h3><a href="{{ item.url }}">{{ item.data.title }}</a></h3>
		<p>{{ item.data.description }}</p>
	</div>
	{% endfor %}
</div>

{% endif %}
