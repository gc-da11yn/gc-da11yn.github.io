---
title: Loi sur l’accessibilité
description: En savoir plus sur la loi sur l'accessibilité et sur le rôle que joue le gouvernement du Canada dans la mise en œuvre de cette loi.
layout: layouts/base.njk
toggle: accessibility-act
tags: accessibilityInTheGovernmentOfCanada
---

{% if collections.accessibilityAct === undefined %}

	{% include "partials/alert-landingpages.njk" %}

{% else %}

<div class="row wb-eqht mrgn-tp-lg">
	{% for item in collections.accessibilityAct | localeMatch(locale) %}
	<div class="col-md-6">
		<h3><a href="{{ item.url }}">{{ item.data.title }}</a></h3>
		<p>{{ item.data.description }}</p>
	</div>
	{% endfor %}
</div>

{% endif %}

