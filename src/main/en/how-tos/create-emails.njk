---
title: Create emails
description: Learn about the essential steps for writing emails accessibly.
layout: layouts/base.njk
tags: howTos
---

<div class="row wb-eqht mrgn-tp-lg">
	{% for item in collections.createEmails | localeMatch(locale) %}
	<div class="col-md-6">
		<h3><a href="{{ item.url }}">{{ item.data.title }}</a></h3>
		<p>{{ item.data.description }}</p>
	</div>
	{% endfor %}
</div>
