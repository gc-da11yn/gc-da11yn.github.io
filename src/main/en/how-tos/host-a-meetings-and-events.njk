---
title: Host a meetings and events
description: Learn how to host accessible virtual meetings such as training session, a kiosk, or an open-door event.
layout: layouts/base.njk
tags: howTos
---

<div class="row wb-eqht mrgn-tp-lg">
	{% for item in collections.hostMeetingsAndEvents | localeMatch(locale) %}
	<div class="col-md-6">
		<h3><a href="{{ item.url }}">{{ item.data.title }}</a></h3>
		<p>{{ item.data.description }}</p>
	</div>
	{% endfor %}
</div>

