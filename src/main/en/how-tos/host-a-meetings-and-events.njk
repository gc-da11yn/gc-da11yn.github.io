---
title: Host a meetings and events
description: Learn how to host accessible virtual meetings such as training session, a kiosk, or an open-door event.
layout: layouts/base.njk
toggle: organiser-des-reunions-et-des-evenements
tags: howTos
---

{% if collections.hostMeetingsAndEvents === undefined %}

	{% include "partials/alert-landingpages.njk" %}

{% else %}

<div class="row wb-eqht mrgn-tp-lg gc-drmt">
	{% for item in collections.hostMeetingsAndEvents | sort(false, false, 'data.title') | localeMatch(locale) %}
	<div class="col-md-6">
		<h2><a href="{{ item.url }}">{{ item.data.title | safe }}</a></h2>
		<p>
			{% if item.data.description %}
			{{ item.data.description | safe }}
			{% else %}
			{{ landingPage[locale].descriptionNoneText }}
			{% endif %}
		</p>
	</div>
	{% endfor %}
</div>

{% endif %}

